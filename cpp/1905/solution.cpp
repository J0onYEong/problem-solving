#include <bits/stdc++.h>
#define ll long long
#define endl "\n"

using namespace std;

void solution() {
    int lx, ly, n;
    cin >> lx >> ly >> n;

    int sum[1002][1002];

    for(int i=0; i<1001; i++) 
        memset(sum[i], 0, sizeof(int)*1001);

    for(int i=0; i<n; i++) {
        int bw, bh, x, y;
        cin >> bw >> bh >> x >> y;

        
    }

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    solution();
    return 0;
}
